{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Compile C++ (GCC) (Debug)",
			"type": "shell",
			"command": "g++ -g -o ${fileBasenameNoExtension} ${file}"
		},
		{
			"label": "Compile C++ (Clang) (Debug)",
			"type": "shell",
			"command": "clang++ -g -o ${fileBasenameNoExtension} ${file}"
		},
		{
			"label": "Compile C (Clang) (Debug)",
			"type": "shell",
			"command": "clang -g -o ${fileBasenameNoExtension} ${file}"
		},
		{
			"label": "Make",
			"type": "shell",
			"command": "make"
		}
	]
}
